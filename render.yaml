services:
  - type: web
    name: news-app
    env: node
    buildCommand: "chmod +x ./build.sh && ./build.sh"
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
